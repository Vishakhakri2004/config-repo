spring:
  application:
    name: auth-service

  datasource:
    url: jdbc:mysql://localhost:3306/authdev_db?useSSL=false&serverTimezone=UTC
    username: root
    password: "YourPassword123!"
    driver-class-name: com.mysql.cj.jdbc.Driver

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    database-platform: org.hibernate.dialect.MySQLDialect

  
  